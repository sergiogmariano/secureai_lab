# Copie para .env e ajuste. NUNCA faça commit do .env real.
APP_SECRET_KEY=gere-32-bytes-aleatorios
JWT_SECRET=gere-outro-segredo-forte
HTTPS_ENABLED=0
# Para usar a IA real (opcional). Sem chave, usa classificador local:
ANTHROPIC_API_KEY=
# Alterna entre app vulnerável (1) e corrigida (0):
VULNERABLE_MODE=0
